#include<stdio.h>

int main(){
    int a, b, w, x, y, z;

    a = 50;
    b = 30;

    w = a + b;
    x = a - b;
    y = a * b;
    z = a / b;

    printf("Sum: %d\n", w);
    printf("Subtraction: %d\n", x);
    printf("Product: %d\n", y);
    printf("Division: %d\n", z);

    return 0;
}
