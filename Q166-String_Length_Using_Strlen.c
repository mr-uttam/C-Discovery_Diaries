#include <stdio.h>
#include <string.h>

int main() {
    // Initialize a string using a pointer
    char *str = "Uttam Pal";

    // Calculate the length of the string using strlen()
    int x = strlen(str);

    // Display the length of the string
    printf("Length of the string: %d", x);

    return 0;
}
